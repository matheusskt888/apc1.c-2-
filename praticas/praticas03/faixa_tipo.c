#include<stdio.h>
#include<float.h>
#include<limits.h>

int main(){
  printf("o tipo'char' aceita valores entre %i e %i\n", CHAR_MIN, CHAR_MAX);
  printf("o tipo 'int' aceita valores entre %i e %i\n", INT_MIN, INT_MAX);
  printf("o tipo 'float' aceita valores entre %i e %i\n", FLT_MIN, FLT_MAX);
  printf("o tipo 'double' aceita valores entre %i e %i\n", DBL_MIN, DBL_MAX);







  
  
  
  
  
  return 0;
}